package com.example.kotlin;

public class BasicsJava {

    // 1. Java variables, immutable/immutable
    String mutable = "mutable";
    final String immutable = "immutable";

    // 2. Java, strongly typed
    String mutableString = "mutable"; // --> String

    // 3. Java, string formatting
    int number = 30;
    String format1 = "number is " + number;
    String format2 = String.format("number is %i", number);
}
